'en':
  activerecord:
    models:
      front_page_lock:
        one: "front page lock"
        other: "front page locks"
        event_already_locked_error: "The event is already locked"
        blog_already_locked_error: "The blog is already locked"
        event_empty_choice_error: "No event chosen"
        blog_empty_choice_error: "No blog chosen"
    attributes:
      front_page_lock:
       event: Event 

  formtastic:
    labels:
      front_page_lock:
        event: Event
