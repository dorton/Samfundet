%h2
  Oppdater anledning

= semantic_form_for @type do |f|
  = f.inputs do
    = f.input :name, label: "Navn:"
    = f.input :description, label: "Beskrivelse:"
  = f.actions do
    = f.submit "Oppdater anledning"
